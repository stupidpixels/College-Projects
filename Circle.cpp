/*Header : *------------------------------------------------------------*| Julio Franco Gonzalez                                      || Estudiante 801.13.2416                                     |*------------------------------------------------------------*Description : *------------------------------------------------------------*| Given the Radious of a cirle, this program will be able to || calculate and display the Diameter, Circumference, Area    || and display the Radious to the user.                       |*------------------------------------------------------------*Pseudocode :  *------------------------------------------------------------*| 1. Ask the user to IMPUT the Radious, store its value in R.||                                                            || 2. Calculate the Area (PI * r^2), store it in A.           ||                                                            || 3. Calculate the Circumferece (2 * PI * R), store it in C. ||                                                            || 4. Calculate the Diameter (2 * R), store it in D.          ||                                                            || 5. Output the Area, Diameter, Circumference and Radious.   |*------------------------------------------------------------**/#include <iostream>using namespace std;int main(){  double PI = 3.14159;  double R;   double A;   double C;   double D;// Ask the user to IMPUT the Radious, store its value in R.   cout << endl << "Give me the radious of the circle : ";   cin >> R;// Calculate the Area (PI * r^2), store it in A.   A = PI * R * R;// Calculate the Circumferece (2 * PI * R), store it in C.   C = 2 * PI * R;// Calculate the Diameter (2 * R), store it in D.   D = 2 * R;// Output the Area, Diameter, Circumference and Radious.   cout << "The Radious of the circle is "<< R << endl;   cout << "The Area of the circle is "<< A << endl;   cout << "The Circumference of the cirlce is "<< C << endl;   cout << "The Diameter of the circle is "<< D << endl;  return 0;}